# Render Postgres / PostgreSQL profile.
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Credentials and JDBC URL are derived at startup from Render's DATABASE_URL (postgresql://...)
# in ExpenseTrackerApplication, unless explicitly provided via SPRING_DATASOURCE_* env vars.

